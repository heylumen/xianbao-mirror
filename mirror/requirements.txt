# mirror/requirements.txt — 渲染脚本依赖锁定
# 安装：pip install -r mirror/requirements.txt
# 随后还需：playwright install --with-deps chromium
playwright==1.61.0
beautifulsoup4==4.15.0
pytest==9.1.1
qrcode==8.2
pillow==12.3.0
